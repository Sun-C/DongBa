#server
server:
  port: 80
  servlet:
    context-path: /
  tomcat:
    threads:
      max: 220
#spring
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/jtsys?serverTimezone=GMT%2B8&characterEncoding=utf8
    username: root
    password: root
  thymeleaf:
    prefix: classpath:/templates/pages/

#mybatis
mybatis:
  mapper-locations: classpath:/mapper/*/*.xml
#log
logging:
  level:
    com.cy: debug
